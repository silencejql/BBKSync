# BBKSync 全程记录（输入要求 / 遇到并解决的问题）

> 覆盖从**初始化开发** → **推广压缩** → **迁移 .NET Framework 4.8** → **打包包含运行环境** → **迁移后修复/优化**的完整过程。
> 阶段 0 / 阶段 D 依据 git 提交记录整理；阶段 A/B/C 为实际操作记录。

---

## 一、输入要求（需求汇总）

| 阶段 | 要求 |
|------|------|
| 0 开发期 | 局域网文件同步与备份工具（BBK 文件同步与备份工具）：源端/目标端（推送、拉取）、文件清单比对、自动备份、FreeForm 进程处理、局域网多电脑互传、常驻监听（端口 25010）、托盘后台运行等 |
| 0 | 单文件发布、体积尽量小（先期目标把 171MB 压下来） |
| 0 | 备份/更新前后日志加分割线，便于阅读 |
| 0 | FreeForm 进程名前缀**可配置**（不硬编码），并支持一键结束远端 FreeFormAlways 进程 |
| 0 | 备份前可选执行 `LocalDB_Backup.bat` 脚本 |
| 压缩发布 | 改为单文件压缩发布，输出从 171MB 降至 72MB |
| 迁移 A/B | 目标框架改为 **.NET Framework 4.8**，回归全部功能，构建/发布/运行/探测通过 |
| 打包 C | 部署包**包含 .NET Framework 4.8 运行环境**，缺失时可离线一键安装并启动 |
| 后续 D | 修复发布版文本框输入崩溃；托盘/窗口图标用程序自带图标；备份前脚本改在**对方电脑**执行；脚本失败不中止备份；优化 Log 输出与默认保存位置；CodeMaid 清理 |

---

## 二、问题与解决记录（按阶段）

### 阶段 0：开发期（2026-09-08，依据 git 提交记录整理）

| 提交 | 需求/问题 | 解决 |
|------|-----------|------|
| 6e708d6 | 初始化 Git | 仓库建立（分支 master / dev / Net4.8） |
| f0bf24c | 增加备份功能 | 加入备份引擎（Add Backup） |
| bb23791 | 中间提交（111） | — |
| 0872509 | 需要把文件传到另一台电脑 | 添加传输文件功能 |
| 6831707 | 备份时文件被占用会报错 | 文件占用报错**标红并跳过后继续**，不中止整次备份 |
| 87f2bc4 | 发布目录维护繁琐 | 新增 publish 整理脚本 |
| 06c6671 | 日志窗口不可调、文件相同信息冗余 | 日志窗口可上下拖拽调整大小；文件相同时简化输出 |
| 884916d | 传输出错时远端无感知 | 传输错误回传远端报错，并确认结束远端 FreeForm 进程 |
| 0986143 | FreeForm 进程偶发未退干净 | 传输失败多轮结束 FreeForm 重试；更新/推送回传远端失败日志 |
| 5b04dde | 同名文件备份会覆盖 | 传输文件备份改为**重命名原文件并保留扩展名**；文件夹备份保持复制不变 |
| 827759d | 日志难定位同步/更新边界 | 备份/更新前后日志增加分割线 |
| 438d13b (dev) | 需要远程结束 FreeFormAlways | 新增“结束远端电脑 FreeFormAlways 进程”按钮 |
| c4547d4 | FreeForm 前缀硬编码、换软件名失效 | 进程名前缀从配置文件读取（可配置 `FreeFormProcessPrefix`） |
| 49909d4 | 备份前需要先跑本地数据库备份脚本 | 备份前可选执行 `LocalDB_Backup.bat`（`RunPreBackupBat`） |

### 阶段 压缩发布（33060a3，2026-09-09 05:08）

| 问题 | 解决 |
|------|------|
| 单文件自包含发布 171MB 过大 | 改为**单文件 + 启用压缩**（`EnableCompressionInSingleFile` 等），输出降至 **72MB** |

### 阶段 A/B：迁移到 .NET Framework 4.8 与验证（d336a40）

#### A 迁移代码改造

| # | 问题 | 原因 | 解决 |
|---|------|------|------|
| A1 | `JavaScriptSerializer` 方案失败：XAML 编译器 `MC1000` 无法解析 `System.Web` | `System.Web.Extensions` 拖入 `System.Web`，WPF 传递依赖链断裂 | 自写零依赖 `MiniJson`（序列化/解析）+ `TypedJson`（POCO 映射）+ `JsonValue`（保持原 `GetProperty/GetString/...` 调用形状，缺失属性返回 null） |
| A2 | `System.Drawing`/`System.Windows.Forms` 全局限定 using 与 WPF 二义性 | `UseWindowsForms=true` 注入 WinForms 全局 using，与 WPF 的 `Application`/`MessageBox`/`SystemColors`/`Brushes` 冲突 | 显式引用两程序集并 `<Using Remove=...>`；`App.xaml.cs` 基类写全限定 `System.Windows.Application` |
| A3 | `init` 属性无法编译 | net48 无 `IsExternalInit` | 新增 `Util/IsExternalInit.cs` polyfill |
| A4 | net10 专属 API | 目标框架差异 | 逐一替换（详见 A5） |
| A5 | net10 API 替换清单 | — | `await using`→`using`；`Memory` 流→`byte[]`+offset；`File.Move(overwrite)`→`PathHelpers.MoveOverwrite`；`Path.GetRelativePath`/`TrimEndingDirectorySeparator`→辅助方法；`TrimEntries`→拆分后逐项 Trim；范围索引→`Substring`；`ConnectAsync(ct)`→两参版；`AcceptTcpClientAsync(ct)`→无参版（用 `_listener.Stop()` 中断）；`Kill(processTree)`→`Kill()`；`Environment.ProcessPath`→`MainModule?.FileName`；`UseDescriptionForTitle` 移除 |
| A6 | csproj 残留 net10 发布属性 | 旧框架配置 | 改为 `net48`/`UseWPF`/`UseWindowsForms`/`LangVersion=latest`/`ImplicitUsings`，引用 `System.IO.Compression(.FileSystem)` |
| A7 | 遗留警告 | 历史代码 | `CS8625`（MainWindow.xaml.cs:52 `null!`）为既有，保留 |

#### B 构建 / 测试 / 发布

| # | 问题 | 原因 | 解决 |
|---|------|------|------|
| B1 | net48 冷启动慢 | 框架依赖 + JIT | 端口约 3~5 秒才监听；`Test-NetConnection` 误报，改 `Get-NetTCPConnection -State Listen` 判定 |
| B2 | 发布体积 | — | 框架依赖发布 `publish-net48\`：**0.28 MB**（exe 292,864B + config 174B），对比原 72MB |
| B3 | 旧配置兼容 | 旧为 net10 缩进格式 settings.json | 实机可读；探针返回 `{"op":"cfg","name":"MBK_3","line":"3"}`；无 crash.log |
| B4 | JSON 正确性 | — | 28 项往返断言全过（五协议 op、long ticks、List<string>、中文/转义、bool false、缺失字段、AppSettings/History 往返） |
| B5 | 协议回归 | — | 临时工程 PeerServer+PeerClient probe+拉取真实文件，内容一致全过 |

### 阶段 C：打包包含运行环境（本次）

| # | 问题 | 原因 | 解决 |
|---|------|------|------|
| C1 | .NET Framework 无法自包含 | 系统组件，不支持自包含 | 启动器 + 同目录离线安装包：缺 4.8 → 静默装 → 启动 |
| C2 | 官方安装包地址 | 需可靠来源 | `https://go.microsoft.com/fwlink/?linkid=2088631` → `ndp48-x86-x64-allos-enu.exe`（121,346,568B），微软官方 |
| C3 | 4.8 检测 | 需精确定位 | 注册表 `...\NDP\v4\Full\Release`，`set /a` 十六进制取十进制与 **528040** 比较（本机 0x8234d=533325 → 判定已装） |
| C4 | 批处理编码（核心难点） | cmd 按控制台代码页读 .bat：UTF-8+`chcp 65001` 不可靠、GBK 在 UTF-8 控制台乱码并解析错乱（多字节吞行）、中文路径跨控制台边界损坏 | **批处理主体纯 ASCII**（任何代码页解析稳定）+ 中文提示放独立 GBK 小文件用 `type` 原样输出 |
| C5 | 安装需管理员 | 提权要求 | `net session` 探测，不足则 `powershell Start-Process -Verb RunAs` 自身提权重跑；已装 4.8 不弹 UAC |
| C6 | 静默安装与校验 | 无人值守 | `/q /norestart`；重跑 is48 校验；1641/3010 = 已装需重启，提示用户 |
| C7 | 缺安装包引导 | 友好提示 | 提示文件 + 官方下载地址 |
| C8 | 测试环境虚假"失败" | ① 工具超时会杀进程树；② 弹出窗口被手动关闭干净退出 | 看门狗式单命令长轮询；结合 WER/事件日志 + crash.log 判定确无崩溃 |
| C9 | 应用正常退出判定 | 无 crash.log/无 WER 事件 | 唯一正常退出路径为托盘“退出”/窗口关闭；未启用自动启动时会显示主窗口 |

**部署包结果**：`publish-net48\`（BBKSync.exe 286KB + config + StartBBKSync.cmd + msg_*.txt 中文提示 + NDP48 安装包 121MB），并打包为 `BBKSync_Net48_部署包.zip`（115.7MB）用于传输。

### 阶段 D：迁移后的修复与优化（master 后续，2026-09-09）

| 提交 | 需求/问题 | 解决 |
|------|-----------|------|
| a43e918 | 发布版文本框输入崩溃；默认图标丑 | 修复崩溃；托盘与窗口图标改用程序自带图标 |
| 744e4d3 | 备份前脚本应在被备份机器上执行 | 局域网备份前脚本改为在**对方电脑**执行 `LocalDB_Backup.bat` 后再拉取备份 |
| 9d497df | 脚本失败把整次备份搞挂 | 备份前脚本执行失败**不再中止**备份：输出本机/远端错误日志后继续 |
| 672cec2 | Log 输出/保存位置不合意；部署产物未入库 | 优化 Log 输出，修改默认保存位置；把 `publish-net48\` 部署产物（启动器/安装包等）纳入版本库 |
| dbd2789 | 代码整洁 | CodeMaid 清理（XAML/CS） |

---

## 三、关键结论 / 部署注意

- 最终形态：**应用 0.28MB + 4.8 离线安装包 121MB ≈ 115.7MB 部署包**。
- 交付物：`publish-net48\`（直接部署）+ `BBKSync_Net48_部署包.zip`（解压后双击 `StartBBKSync.cmd` 一键安装/启动）。
- 目标机器：Windows 7 SP1+；首次装 4.8 自动提权；提示需重启（1641/3010）时重启后使用。
- .NET Framework 是系统组件，体积不可压缩（区别于 .NET Core(.NET 5+) 自包含）。
- 分支：`master`（主线 + 迁移后优化）、`Net4.8`（net48 迁移分支）、`dev`（结束远端 FreeFormAlways 功能）。